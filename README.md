# Cloud Foto / Video

Lokale Cloud-App mit Login, Upload und Wochen-Galerie.

## Start

```bash
cp .env.example .env
# Passwörter und MEDIA_HOST_PATH in .env anpassen
docker compose up -d --build
```

`.env` nicht committen. Vorlage: `.env.example`.
